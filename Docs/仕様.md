# GainForge 仕様（たたき台）

> 本書は初期のたたき台。確定事項と未確定事項を区別して記載する。画面仕様は別途検討。

## 変更履歴

- 2026-06-26: 初版（たたき台）。アーキテクチャ方針と機能要件のドラフト。

---

## 目次

- [概要](#概要)
- [背景・技術的根拠](#背景技術的根拠)
- [アーキテクチャ](#アーキテクチャ)
  - [全体構成（3層）](#全体構成3層)
  - [パッケージ構成（Swift Package）](#パッケージ構成swift-package)
- [GainForgeCore（コアライブラリ）](#gainforgecoreコアライブラリ)
- [CLI ツール](#cli-ツール)
- [GUI ツール（macOS）](#gui-ツールmacos)
- [変換ロジックの要点（移植元の実証結果）](#変換ロジックの要点移植元の実証結果)
- [動作環境](#動作環境)
- [未確定事項・今後の検討](#未確定事項今後の検討)

---

## 概要

GainForge は、Lightroom や DJI / Sony 等が書き出した **HDR ゲインマップ付き JPEG** を、
**ゲインマップを保持したまま HEIC に変換する macOS 向けツール**。

出力は「SDR ベース画像 + ISO ゲインマップ」という、iPhone / 写真アプリと同じ HDR 構造。
SDR ディスプレイでも破綻せず、写真アプリでは元の HDR JPEG と同じ HDR 表示になる。
同等の見た目を保ちつつファイルサイズを削減できる（品質パラメータで調整可能）。

移植元は AISandbox リポジトリの `HDRHEIF/hdrheic.swift`（実証・検証済み）。
GainForge ではこれをライブラリ化し、CLI と GUI から共通利用する。

## 背景・技術的根拠

- 旧来の Core Image `writeHEIFRepresentation(hdrImage:)` 方式は、元のゲインマップを捨てて
  SDR↔HDR 差分から**再計算**するため、ハイライトで色がずれる（写真アプリ書き出しとの平均差分 0.0227）。
- 解法は **ImageIO 低レベル経路で元のカラーゲインマップを Display P3 PQ のまま生転写**すること。
  写真アプリ書き出しの正解 HEIC と画素・コンテナ構造ともほぼ完全一致（平均差分 0.0067〜0.0231）。
- 詳細な調査結果は移植元 `HDRHEIF/調査_色ずれ原因と解法.md` を参照（GainForge にも複製予定）。

## アーキテクチャ

### 全体構成（3層）

```
┌─────────────┐   ┌─────────────┐
│  CLI ツール   │   │ GUI（macOS） │   ← 実行可能ターゲット（薄いラッパ）
└──────┬──────┘   └──────┬──────┘
       │                 │
       └────────┬────────┘
                ▼
       ┌─────────────────┐
       │  GainForgeCore   │            ← 変換ロジック（Swift Package ライブラリ）
       │  ・ゲインマップ生転写  │
       │  ・HEIC 書き出し     │
       │  ・品質/サイズ調整    │
       └─────────────────┘
```

- 変換ロジックは **GainForgeCore** に一元化し、CLI と GUI はそれを import するだけの薄い層にする。
- ロジックを 1 箇所に集約することで、テスト・保守・挙動の一貫性を担保する。

### パッケージ構成（Swift Package）

```
GainForge/
├── Package.swift               # SwiftPM マニフェスト
├── Sources/
│   ├── GainForgeCore/          # ライブラリターゲット（変換ロジック）
│   └── GainForgeCLI/           # 実行可能ターゲット（CLI、Core に依存）
├── App/                        # GUI（macOS / SwiftUI）。Core をローカルパッケージ参照
│   └── GainForge.xcodeproj 等
├── Tests/
│   └── GainForgeCoreTests/     # Core のユニットテスト（変換正当性の回帰）
└── Docs/
    └── 仕様.md                  # 本書
```

- CLI と Core は SwiftPM の `Package.swift` で完結させる。
- GUI（SwiftUI / macOS アプリ）は Xcode プロジェクトとし、`GainForgeCore` を
  **ローカル Swift Package 依存**として参照する（同一リポジトリ内のパッケージを参照）。

## GainForgeCore（コアライブラリ）

変換の中核 API を提供する。最低限のドラフト：

- `func convertToHDRHEIC(input: URL, output: URL, quality: Double, gainScale: Double) throws`
  - ゲインマップ付き JPEG を HDR HEIC に変換（生転写方式）。
- `func hasGainMap(_ url: URL) -> Bool`
  - 入力がゲインマップを持つかを判定。
- ゲインマップ無し画像の SDR HEIC 化（任意機能、CLI の `-f` 相当）。
- エラーは `throws`（`enum GainForgeError`）で表現し、CLI/GUI 双方が扱えるようにする。
  - 移植元は Bool 返却だったが、GUI でのメッセージ表示のため Core では型付きエラーにする。

## CLI ツール

移植元 `hdrheic.swift` の CLI 仕様を踏襲する。

| オプション | 説明 |
|---|---|
| `-q <0.0-1.0>` | HEVC 品質（既定 `0.6`）。SDR ベース画像の圧縮率に作用。ゲインマップは生転写のため不変 |
| `-o <出力先>` | 出力フォルダ（省略時は入力と同じ場所に `.heic`） |
| `-f` | ゲインマップ無し画像も SDR HEIC として変換（既定は警告してスキップ） |
| `<入力 ...>` | ファイル / フォルダ（フォルダは再帰的に `*.jpg` / `*.jpeg` を処理） |

- 出力は `GainForgeCore` の API 呼び出しに置き換える（ロジックは CLI に持たない）。

## GUI ツール（macOS）

- SwiftUI ベースの macOS アプリ。`GainForgeCore` を参照。
- 主軸は macOS（開発機と同一、CLI とコード共有が容易、フォルダ一括変換に向く）。
  将来 iOS へ展開する場合に備え、UI は極力プラットフォーム非依存に保つ。
- **画面仕様は別途検討（本書では未確定）**。想定する操作の方向性のみ記載：
  - JPEG / フォルダのドラッグ&ドロップ受け入れ
  - 品質（`-q`）スライダーと、推定 / 実測ファイルサイズの提示
  - 変換前後のプレビュー（可能なら HDR 表示）
  - バッチ変換の進捗・結果一覧

## 変換ロジックの要点（移植元の実証結果）

GainForgeCore に移植する際、以下の「落とし穴」を必ず維持すること（移植元で実証済み）：

1. ゲインマップは元の補助辞書（`kCGImageAuxiliaryDataTypeISOGainMap`）から
   **Metadata と ColorSpace を取得**する（実ピクセルデータは辞書に含まれない）。
2. ゲインマップ本体は `CIImage(.auxiliaryHDRGainMap)` で**カラー画像として読む**。
3. 焼き込みは **元の ColorSpace（典型: Display P3 PQ）のまま** `render(format: .BGRA8)`。
   sRGB 等で焼くと二重変換で HDR が破綻する。
4. 補助辞書再構成時の `PixelFormat` は **`32BGRA`** に作り直す
   （非公開フォーマット流用は `Finalize` クラッシュ）。
5. ゲインマップ ColorSpace は**ハードコードせず元辞書から取得**（機種ごとに異なるため）。
6. 書き出し後に `hasGainMap` で**検算**し、ゲインマップが実際に埋め込まれたか確認する。

## 動作環境

- macOS（ImageIO / Core Image を使用。ISO ゲインマップ対応のため macOS 15 以降想定）。
- Swift / Xcode。追加ライブラリ不要（Apple フレームワークのみ）。
- Apple Silicon を主対象。

## 未確定事項・今後の検討

- [ ] GUI の画面仕様（レイアウト、操作フロー、プレビュー方式）。← 次に検討
- [ ] Core の公開 API シグネチャ確定（エラー型、進捗コールバックの要否）。
- [ ] ゲインマップ縮小（`gainScale`）をオプション提供するか。サイズ削減目的の任意機能。
- [ ] 移植元ツール／調査ドキュメントを GainForge にどこまで複製・参照するか。
- [ ] バッチ変換時の並列度・キャンセル対応（GUI）。
- [ ] iOS 展開の要否（当面は macOS 中心）。
- [ ] 配布形態（署名 / notarization / App Store の要否）。
